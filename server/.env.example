# Server .env example (do not commit real secrets)

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=seatre

# JWT Configuration
JWT_SECRET=your_jwt_secret_here_change_me_in_production

# Server Configuration
NODE_ENV=development
PORT=3001

# Frontend URL for CORS and email links (dev default)
# - Dev: points to your Nuxt app on localhost:3000
# - Prod: set to your live domain, e.g. https://everyonecancode.net
FRONTEND_URL=http://localhost:3000

# Note: BACKEND_URL is not required by the Express server. It's used by the Nuxt proxy
# configuration in the frontend. Set it in the ROOT .env used by docker-compose instead.
# BACKEND_URL=http://localhost:3001

# SMTP (Email) Configuration — use your WHC/cPanel values
# Typical cPanel settings:
#   SMTP_HOST=mail.yourdomain.com
#   SMTP_PORT=465            # or 587 (set SMTP_SECURE=false for 587)
#   SMTP_SECURE=true         # true for 465 (SSL), false for 587 (STARTTLS)
#   SMTP_USER=noreply@yourdomain.com
#   SMTP_PASS=your_mailbox_password
#   SMTP_FROM=Seatre <noreply@yourdomain.com>
SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Production example (uncomment and edit on server)
# NODE_ENV=production
# FRONTEND_URL=https://everyonecancode.net